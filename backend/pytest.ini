[pytest]
DJANGO_SETTINGS_MODULE = config.settings.local
python_files = test_*.py
testpaths = tests apps
addopts = -q --strict-markers
markers =
    concurrency: tests that exercise database-level race conditions
