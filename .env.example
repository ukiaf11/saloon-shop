# Host port overrides for docker compose. Copy to .env if the defaults clash
# with something already running on your machine (a host Postgres on 5432 is
# the common case).
POSTGRES_HOST_PORT=5432
REDIS_HOST_PORT=6379
BACKEND_HOST_PORT=8000
FRONTEND_HOST_PORT=3000
